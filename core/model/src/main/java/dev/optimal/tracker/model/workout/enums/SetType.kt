package dev.optimal.tracker.model.workout.enums

enum class SetType {
    WARMUP,
    WORKING
}

// TODO: move to dedicated file
enum class ExerciseType {
    REPS_WEIGHT,
    REPS,
    ISOMETRIC,
    ISOMETRIC_WEIGHTS
}